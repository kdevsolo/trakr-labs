DATABASE_URL="postgresql://postgres:postgres@localhost:5432/trakr_labs?schema=public"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/trakr_labs?schema=public"
PORT=3001
# Web app origin — used for invite email redirectTo (must match Supabase Site URL)
# Supabase dashboard: Authentication → URL Configuration
#   - Site URL: same as FRONTEND_URL
#   - Redirect URLs: add {FRONTEND_URL}/auth/callback (and production URL)
FRONTEND_URL="http://localhost:3000"
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
